services:
  bangla-stt:
    build: .
    container_name: bangla-stt-service
    network_mode: host
    volumes:
      - model_cache:/root/.cache/huggingface
    environment:
      - MODEL_SIZE=large
    restart: unless-stopped

volumes:
  model_cache:
